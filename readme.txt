Simple Finit State Machine for parsing a word from Language L based on Alphabet A.
1. Alphabet A is the set {a,b}. 
2. And Language L is defined as words with a certein number of a´s and only one b at the end.

The FSM can parse words like the following (aab, bba, b).
Some accepted words are:
	b,
	ab,
	aaaaaaaaaaaaaab.

Some not accepted words are:

	a,
	bb,
	bab.

